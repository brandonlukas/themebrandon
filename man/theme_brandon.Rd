% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_brandon.R
\name{theme_brandon}
\alias{theme_brandon}
\title{Brandon's Favorite ggplot2 Theme (Light/Dark, Transparent Option)}
\usage{
theme_brandon(base_size = 8, text_size = 7, dark = FALSE, transparent = TRUE)
}
\arguments{
\item{base_size}{Base font size (default: 8).}

\item{text_size}{Font size for most plot text elements (default: 7).}

\item{dark}{Logical, use dark mode? Default is FALSE.}

\item{transparent}{Logical, use transparent backgrounds? Default is TRUE.}
}
\value{
A ggplot2 theme object.
}
\description{
A minimalist theme with balanced text and line size, supporting light/dark modes
and transparent backgrounds for easy PowerPoint embedding.
}
\examples{
ggplot(mtcars, aes(mpg, wt)) + geom_point() + theme_brandon()
ggplot(mtcars, aes(mpg, wt)) + geom_point(color = "gold") + theme_brandon(dark = TRUE)
ggplot(mtcars, aes(mpg, wt)) + geom_point() + theme_brandon(transparent = FALSE)
}
